# Launch Fenrir's installer automatically when the live session starts.
# The check for whether this is actually the live ISO lives in the script
# itself, not here, so this line is a single bare command regardless of
# whether exec-once runs values through a shell or execs them directly.
exec-once = fenrir-installer-autostart
