#!/usr/bin/env bash
# /etc/fenrir-packages.x86_64 exists only on the live image, so this never
# runs on an installed system.
if [ -f /etc/fenrir-packages.x86_64 ]; then
    # No sleep here: if the installer flashes fallback colours on start, restore it.
    exec pkexec-wrapper fenrir-installer
fi
