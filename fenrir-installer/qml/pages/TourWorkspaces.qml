import "../"

TourStepBase {
    stepTitle: "Workspaces"
    body: "Workspaces are extra desktops for grouping windows — press Super + a number (1-9) to jump to one. Windows don't disappear between them, they're just parked on a different workspace until you switch back."
}
