populate_fenrir() {
	if usr/bin/pacman-key -l >/dev/null 2>&1; then
		usr/bin/pacman-key --populate fenrir
	fi
}

post_install() {
	if [ -x usr/bin/pacman-key ]; then
		populate_fenrir
	fi
}

post_upgrade() {
	populate_fenrir
}
