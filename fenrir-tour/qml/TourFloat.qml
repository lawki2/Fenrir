import "../"

TourStepBase {
    stepTitle: "Floating Windows"
    body: "Windows normally tile automatically, filling the screen without overlapping. Open something and press Super + Alt + Space to pop it out of the tiling layout — press it again to snap back in."
}
